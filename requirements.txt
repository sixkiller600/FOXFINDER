# FoxFinder - eBay Deal Notification Service Requirements
# Requires: Python 3.9+ (download from python.org)
# Install: pip install -r requirements.txt

# === System Monitoring ===
psutil>=5.9.0

# === Timezone (for rate limit reset handling) ===
# tzdata: Required for zoneinfo on Windows (Python 3.9+)
tzdata>=2024.1

# === Optional: Colorama for Windows terminal colors ===
colorama>=0.4.6
